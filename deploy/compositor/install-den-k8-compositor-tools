#!/usr/bin/env bash
# Compatibility wrapper for the old den-k8 helper name.
set -euo pipefail
IFS=$'\n\t'

repo_root=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../.." && pwd)
exec "$repo_root/deploy/compositor/install-compositor-tools" "$@"
