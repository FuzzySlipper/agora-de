# Deploy

Deployment is final productization only. Do not put product code here and do not
add legacy bridge scripts to keep the predecessor DE alive.

Current deployment-testing material:

- `shellui/`: systemd/env/runbook material for the first agora-de shell UI HTTP
  service slice.
